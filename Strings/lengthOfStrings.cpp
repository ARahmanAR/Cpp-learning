#include <iostream>
using namespace std;
int main()
{
    string s = "Abdur Rahman";
    cout << s.size() << endl; // s.size() returns the length of the string also we can use s.length() to get the length of the string
    int x = 0;
    cout << (char)x << endl; // 0
}